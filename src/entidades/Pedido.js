class Pedido {
  constructor(id, nome, valor, tipo, foto, status) {
    this.id     = id;
    this.nome   = nome;
    this.valor  = valor;
    this.tipo   = tipo;
    this.foto   = foto;
    this.status = status;
  }
}

export default Pedido;